{
  "name": "nodejs-workflows-samples",
  "private": true,
  "license": "Apache-2.0",
  "author": "Google LLC",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "tsc -p .",
    "fix": "gts fix",
    "lint": "gts lint",
    "test": "c8 mocha --require ts-node/register --extension ts --timeout 600000 --exit"
  },
  "dependencies": {
    "@google-cloud/workflows": "^2.3.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.14.6",
    "gts": "^3.1.1",
    "c8": "^7.13.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1"
  }
}
