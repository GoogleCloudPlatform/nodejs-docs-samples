{
  "name": "workflows-quickstart",
  "description": "Execute a Cloud Workflow",
  "main": "index.ts",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "start": "node --loader=ts-node/esm index.ts",
    "build": "tsc -p .",
    "fix": "gts fix",
    "lint": "gts lint",
    "test": "c8 mocha --loader=ts-node/esm --extension ts --timeout 600000 --exit"
  },
  "dependencies": {
    "@google-cloud/workflows": "^2.3.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.14.6",
    "gts": "^3.1.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1"
  }
}
